import { NavBackDirective } from './nav-back.directive';

describe('NavBackDirective', () => {
  it('should create an instance', () => {
    const directive = new NavBackDirective();
    expect(directive).toBeTruthy();
  });
});
