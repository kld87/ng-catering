import { LabelIngredientCsvPipe } from './label-ingredient-csv.pipe';

describe('LabelIngredientCsvPipe', () => {
  it('create an instance', () => {
    const pipe = new LabelIngredientCsvPipe();
    expect(pipe).toBeTruthy();
  });
});
