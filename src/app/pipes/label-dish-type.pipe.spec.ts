import { LabelDishTypePipe } from './label-dish-type.pipe';

describe('LabelDishTypePipe', () => {
  it('create an instance', () => {
    const pipe = new LabelDishTypePipe();
    expect(pipe).toBeTruthy();
  });
});
